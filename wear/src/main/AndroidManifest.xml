<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.listomni.authenticwatch" >

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/firstwatchface_finished"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault" >

        <activity
            android:name="com.listomni.authenticwatch.AuthenticWatch"
            android:allowEmbedded="true"
            android:label="@string/app_name" >
            android:enabled="true"
            android:taskAffinity=""
            <meta-data android:name="com.google.android.clockwork.home.preview" android:resource="@drawable/firstwatchface_finished" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="com.google.android.clockwork.home.category.HOME_BACKGROUND" />
            </intent-filter>
        </activity>
    </application>

</manifest>
